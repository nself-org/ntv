/**
 * nTV Apple TV — react-native-tvos entry point
 * Registers the root component for tvOS and Android TV.
 */
import { AppRegistry } from 'react-native';
import App from './src/App';

AppRegistry.registerComponent('ntv', () => App);
