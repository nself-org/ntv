# ════════════════════════════════════════════════════════════
# STAGING SHARED CONFIG — NO REAL SECRETS (P96 S101-T04)
#
# Real secrets live in .env.secrets (NOT tracked).
# See .claude/docs/operations/secrets-rotation.md for rotation runbook.
# ════════════════════════════════════════════════════════════
#
# Staging environment overrides
ENV=staging
BASE_DOMAIN=staging.ntv.local
HASURA_GRAPHQL_ENABLE_CONSOLE=false
HASURA_GRAPHQL_DEV_MODE=false
